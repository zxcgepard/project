Как это работает

[Пользователь] → [Telegram] → [Бот] → [Ollama (Qwen2.5)] → [Рецепт]
                    ↑                                    ↓
                 [Сервер Timeweb] ← ← ← ← ← ← ← ← ← ← ← ← ←
